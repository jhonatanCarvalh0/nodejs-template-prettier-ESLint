const numero = '2';
console.info(`numero: ${numero}`);

if (numero === 2 && numero === 2) {
              console.info('data 4');
                                     console.info(`data z${numero}`);
}
